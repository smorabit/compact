% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TargetModulePerturbation.R
\name{TargetModulePerturbation}
\alias{TargetModulePerturbation}
\title{Perturb one co-expression module in a selected population only}
\usage{
TargetModulePerturbation(
  seurat_obj,
  mod,
  perturb_dir,
  perturbation_name,
  target.by,
  target_name,
  graph = "RNA_snn",
  group.by = NULL,
  n_hubs = 5,
  perturb_mode = "zinb",
  n_iters = 3,
  expand_module = 0,
  delta_scale = 0.2,
  row_normalize = FALSE,
  prune_network = FALSE,
  prune_percentile = 0.95,
  corr_sigma = 0.05,
  use_velocyto = FALSE,
  layer = "counts",
  slot = "counts",
  assay = "RNA",
  n_workers = 1,
  custom_network = NULL,
  custom_modules = NULL,
  custom_weights = NULL,
  wgcna_name = NULL
)
}
\arguments{
\item{seurat_obj}{Seurat object containing an hdWGCNA experiment.}

\item{mod}{Co-expression module to perturb.}

\item{perturb_dir}{Perturbation direction/magnitude passed to
\code{ApplyPerturbation}.}

\item{perturbation_name}{Name of the new perturbation assay.}

\item{target.by}{Metadata column defining the cells eligible for perturbation.}

\item{target_name}{One or more values in target.by to perturb.}

\item{graph}{Existing shared cell-cell graph, for example "RNA_snn".}

\item{group.by}{Optional metadata column used only for ZINB model fitting
within the targeted cells. It does not select cells.}

\item{n_hubs}{Number of module hub genes receiving the primary perturbation.}

\item{perturb_mode}{"zinb" or "multiplicative".}

\item{n_iters}{Number of gene-network propagation iterations.}

\item{expand_module}{Number of high-kME grey genes added to a small module.}

\item{delta_scale}{Per-iteration propagation scale.}

\item{row_normalize}{Whether to row-normalize the gene network.}

\item{prune_network}{Whether to prune weak gene-network edges.}

\item{prune_percentile}{Edge-weight percentile used when pruning.}

\item{corr_sigma}{Transition correlation softmax scale.}

\item{use_velocyto}{Retained for call compatibility. Targeted transitions
currently require FALSE so the shared graph can be source-masked before
the sparse correlation calculation.}

\item{layer}{Baseline expression layer used for primary perturbation.}

\item{slot}{Seurat-v4 equivalent of layer.}

\item{assay}{Baseline assay.}

\item{n_workers}{Workers used by the ZINB primary perturbation.}

\item{custom_network}{Optional gene-by-gene network.}

\item{custom_modules}{Optional data.frame with gene_name and module columns.}

\item{custom_weights}{Optional module-table column used to rank hub genes.}

\item{wgcna_name}{hdWGCNA experiment name; defaults to active_wgcna.}
}
\value{
The input Seurat object with a targeted perturbation assay, a
\code{<perturbation_name>_tp} transition graph, a logical source-cell metadata
column, and targeting provenance in \code{@misc$targeted_perturbations}.
}
\description{
A targeted counterpart to \code{ModulePerturbation}. Direct
perturbation and gene-network propagation are restricted to cells selected
by \code{target.by} and \code{target_name}. All other cells remain expression-identical to
baseline. Cell-transition probabilities use the original shared graph, with
targeted cells as sources and all cells retained as possible destinations.
}
\seealso{
\code{\link{ModulePerturbation}}, \code{\link{ApplyPerturbation}},
\code{\link{ApplyPropagation}}
}

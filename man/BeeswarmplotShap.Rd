% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotFindShapKeyDriver.R
\name{BeeswarmplotShap}
\alias{BeeswarmplotShap}
\title{Plot SHAP Beeswarm Summary for Top Driver Genes}
\usage{
BeeswarmplotShap(
  seurat_obj,
  top_n = 20,
  save_plot = FALSE,
  out_dir = NULL,
  filename = "beeswarm_top_shap_DriverGenes.pdf",
  min_color = "#FFCC33",
  max_color = "#6600CC",
  width = 8,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{seurat_obj}{A \code{Seurat} object containing SHAP results from \code{FindShapKeyDriver()},
stored in \code{seurat_obj@misc$shap}.}

\item{top_n}{Number of top driver genes to display (default: 20).}

\item{save_plot}{Save the Beeswarmplot automatically (default: save_plot = FALSE).}

\item{out_dir}{Output directory for the saved plot file. Will be created if it doesn't exist.}

\item{filename}{Name of the output plot file (e.g., \code{"beeswarm_top_shap_DriverGenes.pdf"}).
File format is inferred from the extension (.pdf or .png).}

\item{min_color}{Color for low feature values (default: \code{"#FFCC33"}).}

\item{max_color}{Color for high feature values (default: \code{"#6600CC"}).}

\item{width}{Width of the plot in inches (default: 8).}

\item{height}{Height of the plot in inches (default: 6).}

\item{dpi}{Resolution in dots per inch (only used for raster outputs like PNG; default: 300).}
}
\value{
A \code{ggplot} object representing the beeswarm summary plot.
}
\description{
Generates a beeswarm-style summary plot of SHAP values for the top \code{n} driver genes,
as computed from \code{FindShapKeyDriver()}. This visualization helps interpret gene-level
contributions to model predictions, showing both SHAP value and feature value per cell.
}
\details{
The function extracts SHAP outputs from the \verb{@misc$shap} slot of a Seurat object and
plots per-cell SHAP values for the most important genes. The plot is saved as a PDF
(with rasterized points) or PNG depending on the filename extension.
}
\examples{
# Assuming SHAP analysis was performed using FindShapKeyDriver()
BeeswarmplotShap(seurat_obj, out_dir = "figures/", top_n = 30)

# To save as a raster-aware PDF
BeeswarmplotShap(seurat_obj,save_plot = TRUE,filename = "beeswarm_top_shap_DriverGenes.pdf",out_dir = out_dir)
}
